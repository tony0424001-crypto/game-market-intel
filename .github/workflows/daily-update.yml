name: Daily Game Intel Update

on:
  schedule:
    # Daily at 00:30 UTC (08:30 TST)
    - cron: '30 0 * * *'
  workflow_dispatch:
    inputs:
      run_audit:
        description: 'Force run product audit'
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install feedparser requests

      - name: Run update script
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          if [ "${{ github.event.inputs.run_audit }}" = "true" ]; then
            python scripts/update_data.py --audit
          else
            python scripts/update_data.py
          fi

      - name: Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "Game Intel Bot"
          git add data/
          git diff --staged --quiet || git commit -m "📊 Update $(date -u +'%Y-%m-%d %H:%M')"
          git push
